const validMessagesMock = {
  TITLEMOCKSIZE: 'The length of the "title" is incorrect must be less than 30 characters',
  STATUSERRORMOCK: 'The "status" is wrong must be Pendente, Error, Sucesso or Em andamento',
  DESCRIPTIONERRORMOCK: `The length of the "description" is incorrect must be less than 500 characters`,
};

module.exports = validMessagesMock;
