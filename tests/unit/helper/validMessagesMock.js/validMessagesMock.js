const validMessagesMock = {
  TITLEMOCKSIZE: 'Wrong "title" size must be less than 30 characters',
  STATUSERRORMOCK: `Wrong "status" should be Pendente, Error, Sucesso or Em andamento`,
  DESCRIPTIONERRORMOCK: `Wrong "description" size must be less than 500 characters`,
};

module.exports = validMessagesMock;
